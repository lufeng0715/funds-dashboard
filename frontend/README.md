# funds-dashboard frontend

Vite + React + TypeScript UI for the Phase 1 admin configuration page.

## Scripts

- `npm run dev` starts the local Vite server.
- `npm test` runs Vitest + React Testing Library.
- `npm run build` type-checks and builds production assets.
- `npm run lint` runs ESLint.
